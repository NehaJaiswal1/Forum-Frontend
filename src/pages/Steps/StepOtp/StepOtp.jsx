import React from 'react'

const StepOtp = ({onNext}) => {
  return (
    <>
      <div>
        otp component
      </div>
      <button onClick={onNext}>
        Next
      </button>

    </>
  )
}

export default StepOtp
