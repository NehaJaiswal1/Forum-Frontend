import React from 'react'

const StepUsername = ({onNext}) => {
    return (
        <>
            <div>
                step user name
            </div>
             <button onClick={onNext}>
                Next
            </button>

        </>
    )
}

export default StepUsername
